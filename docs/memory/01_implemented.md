# Implemented — What Worked

Append entries with `apg log --success -m "..."`.
Read this before proposing an approach: if a similar idea already succeeded, build on it rather than inventing from scratch.

<!-- entries below -->
